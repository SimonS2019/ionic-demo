import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter.shapetech',
  appName: 'ionic-demo',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
